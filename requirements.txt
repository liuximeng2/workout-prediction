# PyTorch: use CUDA 12.8 wheels so they match common NVIDIA drivers (e.g. CUDA 12.8 in nvidia-smi).
# Plain `pip install torch` from PyPI can resolve to cu130, which needs a newer driver than many hosts.
--extra-index-url https://download.pytorch.org/whl/cu128

# ── Dataset download ──────────────────────────────────────────────────────────
certifi==2026.2.25
charset-normalizer==3.4.7
idna==3.11
kagglehub==1.0.0
kagglesdk==0.1.16
packaging==26.0
protobuf==7.34.1
pyyaml==6.0.3
requests==2.33.1
tqdm==4.67.3
urllib3==2.6.3

# ── Deep learning core ────────────────────────────────────────────────────────
torch==2.11.0+cu128
torchvision==0.26.0+cu128
safetensors>=0.4.0
huggingface_hub>=0.23.0

# ── VideoMAE / HuggingFace ────────────────────────────────────────────────────
transformers==5.5.0
accelerate>=1.1.0
evaluate==0.4.6

# ── Video processing ──────────────────────────────────────────────────────────
av>=12.0.0

# ── Metrics & visualisation ───────────────────────────────────────────────────
scikit-learn>=1.3.0
imageio>=2.33.0
numpy>=1.24.0
matplotlib>=3.7.0
pandas>=2.0.0

# ── Notebooks (analysis/) ─────────────────────────────────────────────────────
jupyter>=1.0.0
ipykernel>=6.0.0
ipywidgets>=8.0.0
